require 'spec_helper'

describe CsvImportable do
  it 'has a version number' do
    expect(CsvImportable::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
